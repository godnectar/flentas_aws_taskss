1>> Networking & Subnetting (AWS VPC Setup)Task I have created VPC in Region Asia Pacific(Mumbai)

    loggend in to the AWS console then navigated to VPC
    clicked create VPC and More and Created VPC(flentas_vpc)
    select 2 Public 2 Private subnets with CIDR(10.0.0.0/16).
    created IGW and NATGW.
    Edit Route Table for Private Subnet.

